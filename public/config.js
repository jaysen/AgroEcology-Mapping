// AgroEcology Map — runtime configuration
// Edit this file to change settings without rebuilding.
//
// dataUrl: URL or path to the CSV data file. Options:
//   './data.csv'                        — CSV bundled alongside this app (default)
//   'https://docs.google.com/...'       — Google Sheets "publish to web" CSV URL
//   'https://example.com/data.csv'      — any publicly accessible CSV URL
window.AGROMAP_CONFIG = {
  dataUrl: './data.csv',
};
