{
  "name": "agroecology-maps",
  "version": "0.11.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:ghpages": "tsc && GITHUB_PAGES=true vite build",
    "preview": "vite preview",
    "deploy": "npm run build:ghpages && gh-pages -d dist",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@types/papaparse": "^5.5.2",
    "leaflet": "^1.9.4",
    "papaparse": "^5.5.3"
  },
  "devDependencies": {
    "@types/leaflet": "^1.9.8",
    "@types/node": "^20.11.5",
    "gh-pages": "^6.1.1",
    "typescript": "^5.3.3",
    "vite": "^8.0.9",
    "vitest": "^4.1.2"
  }
}
